# Environment variables for GullyKart Backend

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=gullykart
DB_USER=postgres
DB_PASSWORD=password

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRE=24h

# Gmail configuration for sending OTP emails
# Gmail account credentials (use App Password, not regular password)
GMAIL_USER=
GMAIL_APP_PASSWORD=

# Server configuration
PORT=3001
NODE_ENV=development
